
const config = {
  PAGE_TOKEN: process.env.PAGE_TOKEN,
  MESSENGER_GET_STARTED_PAYLOAD: process.env.MESSENGER_GET_STARTED_PAYLOAD,
  PORT: process.env.PORT,
  VERIFY_TOKEN: process.env.VERIFY_TOKEN
}

module.exports = config
